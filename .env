# Astracat DNS Docker Configuration

# Path to the directory containing Let's Encrypt certificates (fullchain.pem, privkey.pem)
# host path -> container path
HOST_CERT_PATH=/etc/letsencrypt/live/dns.astracat.ru

# Unbound Upstream Address (Internal Docker Network)
UNBOUND_UPSTREAM=unbound:53
